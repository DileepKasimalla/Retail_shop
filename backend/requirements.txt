# The real list lives at the repo root — Vercel's Python builder only reads it
# there, and it cannot parse `-r` includes. pip can, so this shim keeps
# `pip install -r requirements.txt` working from inside backend/.
-r ../requirements.txt
