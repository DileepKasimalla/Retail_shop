# In development, leave this blank — Vite proxies /api to http://127.0.0.1:8000.
# For production builds, set this to your backend's public URL (no trailing slash), e.g.
#   VITE_API_BASE_URL=https://api.yourshop.com
VITE_API_BASE_URL=
